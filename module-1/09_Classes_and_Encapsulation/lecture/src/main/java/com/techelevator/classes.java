package com.techelevator;

public class classes {

        /*
    NOTES FROM GREGOR'S LECTURE
    - everything in 'book' class is related to a book
    - access to private variables is only through public functions (properties)

    example, book = DTO (Data Transfer Object)
    private variables, with getters and setters for each

    -----------------------------------------------------------
    Book

    (- = private variables)
    -title
    -isbn
    -author
    -pages

    (+ = public getters and setters = properties - give you access to the private variables)
    + getTitle(): String
    + setTitle(title: String): void

    + getIsbn(): String
    + setIsbn(isbn: String): void

    + getAuthor(): String
    + setAuthor(author: String): void

    + getPages(): int
    + setPages(pages: int): void
    -----------------------------------------------------------
     */

}
